# Optional per-computer overrides for config.local.ini. This file may also
# hold the developer's current task so config.ini stays portable.

[app]
task = cir_monitor
device = follower

[device.follower]
port = auto
serial_number =

[device.leader]
port = auto
serial_number =
